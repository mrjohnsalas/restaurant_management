<%= form_for @credit_card_type, html: { multipart: true } do |f| %>
  <% if @credit_card_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@credit_card_type.errors.count, "error") %> prohibited this credit_card_type from being saved:</h2>

      <ul>
      <% @credit_card_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :logo %><br>
    <%= f.file_field :logo %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
