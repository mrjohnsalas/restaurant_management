<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @credit_card_type.name %>
  <%= image_tag @credit_card_type.logo.url(:medium) %>
</p>

<%= link_to 'Edit', edit_credit_card_type_path(@credit_card_type) %> |
<%= link_to 'Back', credit_card_types_path %>
